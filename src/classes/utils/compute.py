import math

class Compute:
    def __init__(self):
        self.create_run_folder = True

    def calculate_significant_digits_from_data(self, data):
        # push vars to the function, return the maximum # of digits to round to, minimum accuracy by rules
        print(f"No calculations performed")

    def calculate_significant_digits_from_list(self, data):
        # process a list for values, return the maximum # of digits to round to, minimum accuracy by rules
        print(f"No calculations performed")